# 当前交接卡

## 更新时间

- `2026-04-15`

## 当前版本状态

- 当前项目状态：可运行、可继续开发、可验证
- 当前主阶段：`1.0 第一阶段`
- 当前工作性质：核心循环与核心战斗打磨轮，不是地图扩写轮，不是重构轮

## 当前正在解决的问题

- 普通战的“击杀 -> 掉落 -> 吸附 -> 升级”回报链和家族人格已经基本站稳，但仍要防回退
- 当前主问题已经收窄成：精英战要更稳定读成“护卫成屏 -> 裂口 -> 追击本体”的解题型高压层
- `crit / pierce` 在精英窗口里的 live mini-high 还要继续验证自然样本里的稳定度
- 首领收尾里，`屏卫主核` 的后段火线阶段仍保留为持续监控项，避免在战斗打磨中被带坏

## 这轮已经确认的结论

- 战场整体已经比之前更像游戏现场，而不是静态网页叠层
- 路线场地特效、子弹拖迹、敌人受击火花和玩家动作辉光已经能稳定抬高即时读感
- 经验掉落现在更适合继续往“击杀兑现 + 吸附回收 + 追下一只”这条回报链上打磨
- 当前主问题已经从“主循环能不能成立”转成“核心战斗是不是足够爽、足够愿意再打一局”

## 当前不应该做的事

- 不要重写 `RunEngine`
- 不要把项目重新改成复杂大地图
- 不要在没有明确理由时扩新系统
- 不要为了追求爽感去堆纯数值或堆满屏特效
- 不要忽略文档同步，只改代码

## 下一步建议优先级

1. 继续验证精英战里的护卫联动、裂口窗口和自然样本可读性
2. 继续把 `crit / pierce` 的 live mini-high 压到精英窗口与实战即时读感上
3. 顺手复检 `屏卫主核` 火线监控项和路线连续性，避免回退
4. 每次开发后新增复盘，并同步更新本文件

## 接手时必须先看的文档

- `../20_设计闭环/设计基线与约束.md`
- `../20_设计闭环/玩法假设与验证闭环.md`
- `../10_设计文档/流派路线设计.md`
- `../10_设计文档/战斗与首领设计.md`
- `../30_持续优化/测试与验证手册.md`
- `../30_持续优化/开发复盘/` 下最新一篇文档

## 历史追溯入口

- 旧阶段文档统一在 `../90_历史归档/旧版阶段文档/`
- 大量轮次细节优先看：
  - `../90_历史归档/旧版阶段文档/PROJECT_STATUS.md`
  - `../90_历史归档/旧版阶段文档/DEV_ISSUE_LOG.md`

## 2026-04-14 补充进展

- 本轮继续停留在“核心循环与核心战斗打磨”，没有切回关卡可视化、Boss 专项轮或新系统开发。
- 已补强的重点不再是新内容，而是普通战斗里的即时可读性：
  - 低血时新增更明确的边缘危险警示与玩家周围危险圈。
  - 继续保持击杀 -> 掉落 -> 吸附 -> 追下一个目标的回报链节奏。
  - 整体抬高了战斗 BGM 与 `hit / kill / pickup / pressure` 的音量能量。
- 当前可确认的验证结果：
  - `npm run build` 通过。
  - `output/playwright/battle-feel-pass/round11/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - `output/playwright/battle-feel-pass/round11/battle-danger-natural.png` 显示低血危险感已经能读出来，但没有把战场盖成网页式弹层。
  - `window.__pilotAudioDebug()` 运行快照显示 `battle` 音乐模式已在正常出声。
- 当前主线结论不变：
  - 继续做普通战斗的回报链、危险感和节奏爽感。
  - `boss-bastion / fireline` 仍然只是持续监控项，不抢走主线。

## 2026-04-15 补充进展

- 当前主线仍然是“核心战斗继续压实”，没有切回地图、Boss 新机制或大重构。
- 这轮新增的重点已经从“单纯打得更亮”推进到“打得更公平、路线爽点更稳定”：
  - 敌人在出手或撞上玩家后，现在会进入更容易被读出的短破绽窗口。
  - `crit` 更容易把“压低血线 -> 收割 -> 下一枪更快”读成连续小高潮。
  - `pierce` 更容易把“沿线命中 -> 贯穿 -> 清线回收”读成一条连续输出线。
- 当前可确认的验证结果：
  - `npm run build` 通过。
  - `output/playwright/battle-feel-pass/round12/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - `output/playwright/battle-feel-pass/round12/pressure-check/battle-pressure.png` 能看到普通战里更清晰的路线火力和敌我交互层次。
- 下一步建议继续沿这条线做：
  - 优先打磨敌人微行为公平性、路线爽点和拾取节奏。
  - 先别切回地图可视化，也别重新开 Boss 专项轮。

## 2026-04-15 第 2 次补充

- 当前战斗打磨已经继续往“节奏连续性”推进，而不是只停在 hit/kill 特效层。
- 这轮新增成立的重点：
  - 击杀和拾取现在会更直接推动普通战下一拍，不容易刚爽起来就掉回空场。
  - 玩家在 `tempoPulse` 期间更容易追资源、接站位和续压，不会只觉得数字涨了。
  - 普通怪、蛮兵、游击、远程在恢复期里的退让和再压上方式更不一样，开始更像四类对手。
- 当前可确认的验证结果：
  - `output/playwright/battle-feel-pass/round13/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - `output/playwright/battle-feel-pass/round13/pressure-check/battle-mid.png` 能看到战场在普通样本里也保持连续压力，没有明显回退成网页式停顿体验。
- 下一步建议继续沿这条线做：
  - 优先继续做敌人微行为人格、精英压迫节奏和路径回报链。
  - 先别把主线切回地图、Boss 新内容或大 UI 轮。

## 2026-04-15 第 3 次补充

- 当前主线仍然是“核心循环 / 核心战斗打磨”，没有切回地图可视化、Boss 专项或新系统开发。
- 这轮新增成立的重点已经从“普通战不空场”继续推进到“普通战 / 精英战更像两种不同层次的压力”：
  - 精英现在开始拥有更明确的压迫脉冲、短收招窗口和更可读的再压上节拍，不再只是更厚的普通战。
  - `crit` 与 `pierce` 在实战里多了一层更稳定的 mini-high：
    - `crit` 更容易在精英短窗口里读成“压线 -> 追收 -> 继续开火”。
    - `pierce` 更容易在精英护卫线或普通敌群里读成“拆线 -> 打穿 -> 继续滚动”。
  - 普通敌人开始更像家族协同而不是四种单兵：
    - `standard` 更会贴着前排补线。
    - `brute` 更会带着支撑怪一起顶线。
    - `skirmisher` 更像借掩护绕侧切回。
    - `ranged` 更像站在掩护后借屏开火。
- 当前可确认的验证结果：
  - `npm run build` 通过。
  - `output/playwright/battle-feel-pass/round14/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - `output/playwright/battle-feel-pass/round14/full-flow/battle-opening.png`、`battle-mid.png` 与 `output/playwright/battle-feel-pass/round14/elite-check/battle-late-1.png`、`battle-late-2.png` 能看到普通战家族压迫和路线即时读感继续抬高。
- 当前阶段判断：
  - 普通战已经不只是“能玩”，而是开始有可持续回报链和家族压迫人格。
  - 当前最大缺口已经收窄成：精英战虽已开始拉开层次，但还需要继续验证它在自然样本里是否足够稳定地被读成“解题型高压场”。

## 2026-04-15 第 4 次补充

- 当前主线仍然停留在“核心循环 / 核心战斗打磨”，没有切回地图、Boss 新机制或新系统开发。
- 这轮新增成立的重点继续压在精英战层次上：
  - 精英脉冲现在会带着附近护卫一起成形，不再只剩精英本体自己在顶压。
  - 精英收招后，附近护卫会更明确地一起进入短裂口，战场读法更接近“先拆屏，再追本体”。
  - `crit / pierce` 会更稳定吃到这类裂口窗口，而不是只在普通战里有 mini-high。
- 当前可确认的验证结果：
  - `npm run build` 通过。
  - `output/playwright/battle-feel-pass/round15/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - `output/playwright/battle-feel-pass/round15/general-check/battle-opening.png` 与 `battle-pressure.png` 没有出现新的画面回退，普通战读感保持稳定。
- 当前仍保留的风险：
  - 定向精英自然样本截图脚本还没有稳定命中，因此这轮对“精英裂口窗口是否已在自然样本里足够常被读到”的验证仍然不算完全签收。
- 当前阶段判断：
  - 普通战层已经基本稳住，主缺口继续收窄为精英战的自然样本稳定度与可读性。

## 2026-04-15 第 5 次补充

- 当前主线仍然留在“核心循环 / 核心战斗打磨”，没有切回地图、Boss 专项或新系统开发。
- 这轮继续只压精英战的“裂口 -> 追本体”读法，没有重开普通战大改：
  - 护卫在精英收招后的裂口不再只是逻辑进 recovery，而是会更明确地侧退、后撤，把追击路让出来。
  - 自动火力在精英裂口期间会更愿意锁向本体，而不是继续被护卫把主味抢走。
  - 战场里新增了一条更直观的裂口追击楔形提示，让“现在能穿进去”更容易被直接看懂。
- 当前可确认的验证结果：
  - `npm run build` 通过。
  - `output/playwright/battle-feel-pass/round16/full-flow/summary.json` 中 `resultSeen = true`、`replaySeen = true`、`consoleErrors = []`。
  - 定向精英 rerun 已能确认 `eliteRouteChosen = true`、`eliteBattleSeen = true`。
- 当前仍保留的风险：
  - 精英战自然样本已不再是“打不到”，但精确裂口帧截图仍有一定时机波动，后续还要继续复检。
- 当前阶段判断：
  - 普通战层保持稳定。
  - 当前主缺口继续收窄为精英裂口窗口的自然样本签收，而不是玩法方向本身还没成立。
