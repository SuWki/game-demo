# 当前待办与优先级

## 更新时间

- `2026-05-04`

## 2026-05-04 当前最高优先级

**下一轮推荐主题**：`路线牌机制真实化 + 三路线实机验收`。

**当前真实状态**：
- `crit / pierce / dash` 已有敌人受击层机制骨架：破绽层数、裂纹层数、脉冲层数。
- 敌人受击表现已接入：暴击橙色爆闪、穿透蓝色裂纹、穿梭绿色涟漪。
- 玩家周围路线浮动文字系统已删除，不能再回到“暴击 / 贯穿 / 脉冲”大字提示。
- 路线价值审计当前为硬违规 0、超预算 0。

**仍不可签收为完整构筑**：
- 当前机制主要是全局骨架，不是每张路线牌都有真实差异。
- 少数高品质牌已经改成更谨慎的描述，但大多数路线牌仍偏“核心属性累积”。
- 下一轮必须验证和推进“卡牌之间如何组合”，而不是继续把路线牌当更强数值牌。

**下一轮 P0**：
1. 人工实机 3 局，分别优先走 `crit / pierce / dash`，记录路线牌数量、成型节点、精英/Boss 击杀速度、玩家主要死因。
2. 观察敌人受击特效：橙色破绽、蓝色裂纹、绿色脉冲是否读得出来，是否遮挡弹幕和敌人。
3. 挑每条路线 3 到 5 张关键路线牌做真实机制接线或描述回收。
4. 检查是否仍有“卡牌描述承诺了机制，但代码没有实现”的情况。
5. 继续保证升级面板最多 1 张路线牌、路线审计硬违规 0、超预算 0。

**下一轮不做**：
- 不全量重写 64 张路线牌。
- 不靠堆怪物血量解决流派强度。
- 不恢复玩家周围常驻线条或大字路线提示。
- 不先转作品集材料。

## 2026-05-03 无上下文接手后的下一步

**下一轮推荐主题**：`流派构筑第三轮 + 路线表现迁移到敌人受击层`。

**为什么先做这个**：
- 当前预算、抽卡规则、tooltip、Boss QA 都已基本接通，继续小修数值收益变低。
- 用户最关心的是“流派不是拿一张牌就算流派”，以及“战斗表现不要靠文字和玩家周围线条解释”。
- 因此下一轮应把 `crit / pierce / dash` 的构筑和表现一起推进。

**下一轮最小范围**：
1. 每条路线只改 3 到 5 张关键路线牌，不全量重写 64 张。
2. 蓝 / 紫 / 金牌优先改成条件收益、承接收益、终局兑现。
3. 敌人受击层补路线特效：暴击破绽爆闪、穿透蓝色裂纹/入射出射痕、穿梭绿色相位扰动。
4. 战斗中减少“暴击 / 贯穿 / 脉冲”等文字，保留 tooltip 解释。
5. 验证升级面板最多 1 张路线牌、路线审计仍为硬违规 0、超预算 0。

**仍需实机确认**：
- 玩家周围是否仍有残留线条。
- 敌人状态标记是否可读、不遮挡。
- Boss 胜利路径收尾是否清楚。
- `crit / pierce / dash` 是否仍会 1 到 2 张牌杀穿精英或 Boss。

## 2026-05-03 Kimi 修复验收与 Codex 补刀后状态

**已完成**：
- Kimi 回调 4 张超预算路线牌，`audit-route-upgrade-value.mjs` 当前显示硬违规 0、超预算 0。
- Kimi 删除了部分玩家周围暴击/穿透常驻线条。
- Codex 复验发现仍有 `liveFocusRoute === 'crit' / 'pierce'` 玩家周围读局线，已继续删除。
- 当前路线签名应主要通过敌人状态标记、弹道和命中反馈表达，不再依赖玩家周围常驻线条。

**仍不可签收**：
- 本轮只是路线价值回调，不是完整流派构筑重构。
- 玩家周围是否仍有其他线条来源，需要 full-flow QA 和人工实机确认。
- 三路线仍需要从“数值型强化”继续改成“低品质组件 + 中品质承接 + 高品质转换/兑现”的构筑链。

**下一轮优先级**：
1. 跑 full-flow QA，确认本轮补刀没有引入 console error。
2. 人工实机走一局 `pierce`，重点确认玩家周围是否还有非必要线条。
3. 开始路线构筑第三轮：优先改蓝/紫/金路线牌，让它们提供条件收益、触发方式变化、终局兑现，而不是更高数值。
4. 如果实机仍觉得暴击弱，优先增强“破绽承接”，不要直接堆暴击率或爆伤。

## 2026-05-03 本轮实施结论

## 2026-05-03 Codex 处理用户 5 项反馈后状态

**已修复**：
- 战斗节点卡片描述不再统一成“打一场精英战，继续推进”，改为显示节点自身说明。
- 强化卡正文不再塞入专属名词长括号解释，专属名词解释改回浮动 tooltip。
- `穿梭 / 脉冲 / 无伤窗口 / 暴击 / 穿透` 的 tooltip 已补充触发条件，而不是只写强化效果。
- 通用强化同属性同屏重复已加兜底：一次升级选择不应再同时出现两张“移速”或两张同类型通用强化。
- 通用强化同属性同品质已固定基准值：同为白 / 绿 / 蓝等品质时，同属性不应再出现明显不同数值。
- 普通升级、节点整备、最终整备统一走“路线牌最多 1 张”兜底。
- 协作规则已补充：用户编号问题必须在下一次最终回复中逐条带回问题和解决方案。

**不能签收为完成构筑**：
- 当前路线仍只是初步分层和价值审计，不是完整构筑玩法。
- 后续仍要把三路线改成“低品质组件 + 中品质承接 + 高品质转化/兑现”的组合链，而不是只叠数值。
- 穿透流玩家周围残留线条仍需单独追踪 `GameScene.ts` 视觉层。

**下一轮优先级**：
1. 实机确认本轮升级面板是否还会出现两个同属性通用强化或两个路线牌。
2. 实机确认 tooltip 是否解决“我不知道穿梭怎么触发”的问题。
3. 单独排查穿透流玩家周围线条残留，不要误删敌人状态标记。
4. 开始三路线构筑重构设计：每条路线至少拆成“入口组件 / 承接组件 / 条件收益 / 终局兑现”。

## 2026-05-03 流派构筑实机回归与表现层去噪结论

**已完成修复**:
- ✅ `tools/qa-boss-directed-v2.mjs`: 修复 `panelBlockedMoments` 误判问题
  - 增加 `closeUpgradeOrOverlayIfPresent` 调用后的等待时间（400ms → 600ms）
  - 增加 DOM 更新等待（200ms）
  - 面板遮挡检测改为 Playwright `page.locator` 实时查询
  - 增加面板关闭重试机制，确认面板确实仍然存在才计数
  
**可以签收**:
- Boss 收尾 UI（失败路径）已通过 `qa-boss-ending.mjs` 自动验收。
- 路线价值审计硬违规 0，超预算 0，健康度 100%。
- 三个 Boss 定向 QA 全部通过，`panelBlockedMoments=0`：
  - `boss-bastion`: 安全区 5，区外伤害 14，区内清弹 56
  - `boss-hunt`: 安全区 4，区外伤害 14，区内清弹 11
  - `boss-lockdown`: 安全区 4，区外伤害 14，区内清弹 11

**仍需实机确认**:
- Boss 击败胜利路径的收尾文字与节奏。
- 三路线是否会被 1 到 2 张牌杀穿精英/Boss。
- 敌人状态标记（暴击橙环、穿透蓝裂纹、穿梭绿脉冲）在真实混战中的可读性。

**关于 `boss-lockdown panelBlockedMoments=1` 问题**:
- **根因**: QA 脚本时序问题，面板关闭后 DOM 更新延迟导致误判。
- **修复**: 增加等待时间和重试机制，改用 `page.locator` 实时查询。
- **验证**: 当前三个 Boss 定向 QA 均为 `panelBlockedMoments=0`。

**下一轮 P0 顺序**:
1. 人工实机至少 3 局，分别优先走 `crit / pierce / dash`，记录路线牌数量、成型时机、精英/Boss 是否被快速秒杀。
2. 验证敌人状态标记可读性：暴击橙环、穿透蓝裂纹、穿梭绿脉冲是否遮挡战斗画面。
3. 如路线仍过早成型，回调路线牌数值或调整分发权重。
4. 如表现层遮挡严重，降低状态标记透明度/持续时间。

**可以签收**：
- Boss 收尾 UI 已修复：真实生效的 `syncOverlay()` 已包含 `bossEnding` 分支。
- `tools/qa-boss-ending.mjs` 已改为使用 `__pilotQaForceBoss` / `__pilotBattleDebug`，可真实验证 `.boss-ending-screen`。
- `boss-lockdown` 失败路径收尾已自动验收通过，显示“机体失效 / Boss 未击破”，`consoleErrors=0`。
- 路线价值审计已归零：硬违规 0，超预算 0，硬规则健康度 100%，预算健康度 100%。
- Full-flow QA 通过；三个 Boss 定向 QA 均能采到安全区和区外伤害数据，但 `boss-lockdown` 有 `panelBlockedMoments=1`，下轮需要判断是 QA 入口残留面板还是实际流程遮挡。

**仍需实机确认**：
- Boss 击败胜利路径收尾是否足够清楚。
- `boss-lockdown` 定向 QA 的 1 次面板遮挡是否为脚本误判或真实开场遮挡。
- 敌人状态标记在真实混战中是否可读、不遮挡、不像噪音。
- `crit / pierce / dash` 是否仍然过早成型或 1 到 2 张牌杀穿精英/Boss。

**下一轮 P0 顺序**：
1. 人工实机至少 3 局，分别优先走 `crit / pierce / dash`，记录路线牌数量、成型时机、精英/Boss 击杀速度和主要死因。
2. 复查 `tools/qa-boss-directed-v2.mjs` 的面板遮挡判断，重点看 `boss-lockdown` 的 `panelBlockedMoments=1` 是否可复现。
3. 检查敌人状态标记：暴击橙环、穿透蓝裂纹、穿梭绿脉冲是否真正帮助读局。
4. 如果仍然杀穿，优先回调路线牌分发与敌人出题，不要先堆怪物血量。
5. 如果表现遮挡，优先降低状态标记透明度/持续时间/数量，不要回到玩家周围大浮字和常驻线条。

## 2026-05-03 Codex 复验补充

**可以签收**：
- 路线价值审计脚本已能计算负数收益，`dashInterval` 不再漏算。
- 暴击标记结算顺序已修正，不再同一次命中自触发。
- 敌人状态标记可视化已接入：暴击橙环、穿透蓝裂纹、穿梭绿脉冲。
- Full-flow QA 通过，三个 Boss 定向 QA 通过，Boss 安全区数据仍可采样。

**不能签收**：
- Boss 收尾画面未真实显示。`RunEngine` 已进入 `bossEnding`，但 `screen-layer` 仍是 `hidden`，`.boss-ending-screen` 数量为 0。
- 直接原因是 `GameScene.ts` 中有两个 `syncOverlay()` 定义，后面的生效版本缺少 `bossEnding` 分支，覆盖了前面新增的 Boss 收尾逻辑。
- 路线审计仍有 4 张超预算：`crit-branch-ignite`、`dash-brush`、`dash-loop`、`dash-rethread`。
- 审计报告口径仍要修：总结显示 4 张超预算，但重点修正列表只列 3 张，遗漏了 `crit-branch-ignite`。

**下一轮 P0 顺序**：
1. 合并/清理 `GameScene.ts` 的重复 `syncOverlay()`，确保真实生效版本包含 `bossEnding` 分支，并补一个 QA 探针验证 `.boss-ending-screen` 真实出现。
2. 修复审计报告重点列表，确保所有超预算牌都列入待修正。
3. 回调 4 张超预算牌，优先 `dash-loop`、`dash-brush`，再处理 `dash-rethread` 和 `crit-branch-ignite`。
4. 人工实机验证敌人状态标记是否可读且不遮挡，不再用玩家周围常驻线条解释流派。

**可以签收**：
- 路线价值审计脚本已修复，支持负数、小数、多 modifiers 块，硬规则健康度 100%。
- 暴击标记结算顺序已修正，先读取旧标记再刷新新标记，避免同一次暴击立即受益。
- 敌人状态标记可视化已实现：暴击橙色破绽环、穿透蓝色贯穿裂纹、穿梭绿色脉冲残影。
- Boss 战收尾引擎状态已实现，但 UI 未真实显示，不能签收。
- 构建验证通过。

**不能签收为最终体验**：
- 路线牌仍有 4 张超预算：`crit-branch-ignite`、`dash-brush`、`dash-loop`、`dash-rethread`。
- Boss 收尾画面需要先修 UI 不显示问题，再谈样式优化。
- 敌人状态标记的实际战斗可读性需人工实机验证。

**下一轮 P0 顺序**：
1. 修复 Boss 收尾 UI 不显示问题。
2. 调整超预算路线牌（优先处理 dash 路线）。
3. 人工实机至少 3 局，分别优先走 `crit / pierce / dash`，验证流派构筑体验。

**已完成**：

- ✅ Boss QA 真实采样修复完成：
  - `tools/qa-boss-directed-v2.mjs` 面板关闭逻辑已修复，支持 `[data-choice]`
  - 三个 Boss QA 全部通过：`boss-bastion`, `boss-hunt`, `boss-lockdown` 均采到安全区数据
  - 安全区时刻均 >= 4，区外伤害 >= 10，区内清弹 >= 8，面板遮挡 = 0

- ✅ 流派构筑第二轮骨架已完成：
  - `src/game/types.ts`: 新增 `EnemyState.critMarkSec/pierceMarkSec/dashMarkSec`
  - `src/systems/RunEngine.ts`: 
    - 敌人初始化添加标记字段
    - 暴击命中设置 `critMarkSec` 并追加 15% 连击伤害
    - 穿透命中设置 `pierceMarkSec`
    - Dash 脉冲命中设置 `dashMarkSec`

- ✅ 路线价值审计：64 张路线牌，0 违规，3 张超预算，健康度 100%

- ✅ 构建验证通过

**未完成的 P1**：

- ⚠️ Boss 收尾画面：本轮未完成，移至下一轮
- ⚠️ 敌人状态标记渲染：逻辑已完成，GameScene 视觉表现待实现

## 当前总判断

当前项目已完成 P0 实机回归修复：
- ✅ `finalPrep` 路线牌上限兜底已修复
- ✅ 横向车道残留图层已删除
- ✅ Boss 安全区尺寸收窄约 20%
- ✅ Boss 安全区外 tick 伤害 + 视觉/音效反馈已增强
- ✅ 安全区内清弹计数已记录
- ✅ 专属名词解释已补充到 `formatModifierLabel`
- ✅ `audit-route-upgrade-value.mjs` 已修复为静态解析 TypeScript
- ✅ `qa-boss-directed-v2.mjs` 已添加自动关闭面板 + 安全区数据采集
- ✅ 路线牌违规问题已全部修复（健康度 100%）
- ✅ `outsideSafeDamageTicks` 已拆分为 `outsideSafeDamageTimerSec` 和 `outsideSafeDamageTickCount`
- ⚠️ Boss 战收尾画面：本次未实现（需 UI 层改动，建议下一轮处理）
- ⚠️ 暴击路线卡顿：本次未专项定位（需性能样本，建议用户实机后反馈）

作品集线继续暂缓。下一步等待用户实机反馈当前版本。

## P0：最新实机回归问题（2026-05-02 Kimi 已完成）

### 已完成修复

- ✅ `finalPrep` 路线牌上限：`contentSelectors.ts` finalPrep 分支添加 `limitRouteCardsInUpgradeChoices` 兜底
- ✅ 横向残留图层：`GameScene.ts` `renderEncounterBackdrop` 和 `renderEncounterFlowOverlay` 的 `lanes + horizontal` 分支已注释删除
- ✅ Boss 安全区尺寸：`RunEngine.ts` `openPressureSafeWindow` 安全区尺寸收窄约 20%
- ✅ Boss 安全区外惩罚：`RunEngine.ts` `applyBossSafeWindowPenalty` 改为 tick 伤害（0.35s 间隔）+ 视觉/音效反馈
- ✅ 安全区内清弹：`RunEngine.ts` 记录 `insideSafeProjectileClears` 到 `BattleState`
- ✅ 专属名词解释：`upgrades.ts` `formatModifierLabel` 添加暴击率/穿透/穿梭冷却/无伤窗口触发说明
- ✅ 路线价值审计：`audit-route-upgrade-value.mjs` 改为静态解析 `upgrades.ts`，可真实运行
- ✅ Boss QA 脚本：`qa-boss-directed-v2.mjs` 添加自动关闭面板 + 安全区数据采集（`bossSafeWindowMoments` / `outsideSafeDamageTicks` / `insideSafeProjectileClears`）
- ✅ 超预算路线牌：`crit-aim` 和 `dash-anchor` 已修复混入泛属性问题

### 待用户实机确认

- Boss 战收尾画面（需 UI 层支持，P1 处理）
- 暴击路线卡顿（需用户实机反馈后定位）
- 剩余 10 张超预算/违规路线牌（健康度 81.3%，可继续回调）

### 暂不签收的上一轮内容

- “升级面板最多 1 张路线牌”只覆盖了部分路径，`finalPrep` 仍有风险。
- “路线牌价值收窄”仍不彻底，部分路线牌仍混入泛属性。
- “Boss 安全区压力增强”没有通过有效 QA 和用户实机。
- “玩家周围线条去除”只去掉了一部分，关卡横向辅助层仍干扰画面。
- “专属名词解释”仍没有达到玩家无文档可理解的程度。

## P1：流派构筑与敌人出题验收

目标：确认当前版本是否已经从“拿几张强牌杀穿”回到“通过构筑形成打法，再用打法解决精英/Boss 出题”。

### 2026-05-02 最新实机反馈

用户反馈当前最新版本仍存在以下 P0 问题：

- 流派增益出现概率过高，普通强化缺乏性价比。
- 专属名词浮动窗只告诉玩家“穿梭有什么强化”，没有解释“怎样才算穿梭、怎样会触发”。
- 开局偶发玩家周围有 3 条绿色横线固定跟随，需要去掉。
- 一次升级选择仍可能出现两个流派 buff。
- Boss 生存压力偏小，需要适度增加弹幕数量、缩小安全区，并提高安全区外真实伤害压力。
- 开局拿 `伤害 +4` 后再拿暴击流派增益会卡死，需要优先修复。
- 路线选择中仍出现两个强化关卡，必须写成硬规则：战斗节点可以多个，强化/异常节点最多一个，且概率远低于战斗。
- 暴击路线风险/收益方向是对的，但路线牌同时给暴击和爆伤，性价比明显高于普通强化。
- 穿透路线强度仍然无敌，需要按“构筑分层 + 条件触发 + 价值预算”重构。

下一轮 Kimi 应先修确定性 bug：卡死、节点数量、升级面板路线牌数量、玩家周围绿色横线。随后再做流派重构和 Boss 压力回调。

必须完成：

- 人工实机至少 3 局，分别优先走 `crit / pierce / dash`。
- 记录每局路线牌数量、是否成型、精英/Boss 是否被快速秒杀、死亡/胜利节点和主要感受。
- 确认 1 张路线牌只形成倾向，不形成完整流派。
- 确认 2 到 3 张路线牌开始有承接，但不能稳定跳过精英/Boss。
- 确认 4 张以上路线牌才开始形成稳定打法。
- 确认 Boss/精英能体现不同路线解法，而不是所有路线都靠 DPS 打穿。
- 确认素材和音效接入没有牺牲读局。
- 确认路线触发浮字和玩家周围线条是否仍然干扰画面，如果干扰，下一轮优先移除。

重点看：

- `crit` 是否通过短窗、弱点、爆点成立，而不是平均 DPS 碾压。
- `pierce` 是否通过角度、敌线、护卫和贯穿成立，而不是无脑清屏。
- `dash` 是否通过换位、安全窗、回切和反打成立，而不是冷却低就安全。
- Boss/精英是否像出题单位，而不是血厚木桩或数值沙包。
- 战斗画面是否干净，玩家周围是否还有不必要的线条。

## P2：表现层与手感第二轮补强

目标：在 P0 实机反馈明确后，再继续补画面读局、打击反馈、音效层次、操控承接和通俗提示。

优先方向：

- 检查 `src/scenes/GameScene.ts` 的弹道、命中、击杀、拾取、危险提示、路线签名是否清楚。
- 检查 `src/systems/PilotAudio.ts` 的主反馈链、危险链、路线声纹是否分得开。
- 检查 `src/ui/OverlayController.ts` 和 `src/style.css` 的开始页、暂停页、升级、结算、局内提示是否通俗且不遮挡。
- 小步优化玩家操作手感：移动起停、反向转向、拾取吸附、开火承接、击杀后继续开火/追击。
- 把路线触发文字逐步改为敌人身上的特效、命中点反馈和短时弹道反馈。
- 去掉玩家周围长期或半长期的解释性线条，让战斗画面更干净。

## P3：关卡节奏与普通战回报链复检

目标：在 `f9fe44d` 实机验收和表现层补强后，再确认关卡节奏不是“感觉更快”，而是真正让核心循环更粘。

优先方向：

- 观察 `killPickupContinueMoments` 或类似 debug 字段是否保持/提升。
- 观察自然样本第 2~3 节点是否仍容易被打断。
- 观察路线倾向是否更早可见，但没有过早锁死。
- 观察升级卡是否更像回应上一场战斗，而不是孤立数值选择。

验收重点：

- 战斗爽感来自回报链和读局，而不是单纯调高数值。
- 中段站稳来自路线承接，而不是早早发终局强牌。
- 后段收束来自节点职责和压力变化，而不是堆怪。

## P4：文档口径收口

目标：让接手入口和当前待办反映 2026-04-29 后的新协作方式和当前验收重点。

需要维护：

- `doc/00_接手入口/当前交接卡.md`
- `doc/00_接手入口/START_HERE.md`
- `doc/30_持续优化/当前待办与优先级.md`
- `doc/30_持续优化/测试与验证手册.md`
- `doc/40_AI协作/Codex_Kimi协作分工规则.md`

当前规则：

- Codex 默认只做方向、文档、素材/音效草案和 Kimi 提示词。
- Kimi 默认做代码开发、构建验证、浏览器 QA 和开发复盘。
- 新的开发提示词默认直接在对话中输出，不再新增提示词文档。
- 文档只沉淀项目事实、设计约束、验收标准、复盘结论。
- 历史复盘只用于追溯，不再作为当前优先级来源。

## P5：作品集材料三件套

目标：把当前项目转成战斗策划作品，而不是继续做宽。当前暂缓，等表现层和手感更稳后再继续。

优先产出：

1. `项目总览一页纸`
2. `战斗参数表`
3. `敌人设计表`

为什么先做这三件：

- 它们能最早暴露“当前项目还缺哪些硬证据”。
- 它们能帮助判断是否真的需要新增 Boss 或精英。
- 它们比继续写大文档更接近可投递材料。

当前补充：

- `精英战案例页` 已经用于承接精英裂口与护卫拆解案例。
- `Boss战案例页` 已经用于承接 Boss 火线、安全窗与三路线答题案例。
- 下一步不是继续写长文，而是给这些材料补标注截图、短视频脚本和可投递排版。

## P6：素材与音效同步优化

目标：服务作品集展示和实机观感，不抢战斗设计主线。

允许做：

- 首批核心视觉资产预览：玩家、标准敌、精英核心、Boss Bastion、XP 球、基础弹道。
- 首批核心音效替换或制作：`shoot / hit / kill / pickup / hurt / nearMiss / pressure / enemyShot`。
- 演示视频需要的 UI / 结果页 / 战斗关键帧截图优化。

暂不建议做：

- 全量正式素材替换。
- 大规模 UI 主题重做。
- 把项目包装成美术或音频作品集。
- 为了素材好看牺牲战斗读局。

## P7：是否新增 Boss / 精英

暂时不建议立刻开做。

触发条件：

- `项目总览一页纸`、`战斗参数表`、`敌人设计表` 做完后，发现当前 Boss 或精英案例说服力不足。
- 演示视频脚本无法用现有 Boss / 精英讲清楚三路线答题方式。
- 当前硬数据不足以支撑作品集案例页。

如果满足上述条件，再考虑：

- 新增 1 个更考路线空间的 Boss。
- 新增 2 个分工明确的精英模板。

## 当前明确不做

- 不重写主流程。
- 不重写 `RunEngine`。
- 不扩成长线局外系统。
- 不做全套正式资产替换。
- 不把作品集做成几十篇文档合集。
- 不把 AI 协作文档当成核心卖点。

## 下一步推荐

1. 下一轮先让 Kimi 做 P0 回归修复，不再写作品集材料。
2. Codex 输出提示词时必须包含代码骨架，不只写方向。
3. 只修最新实机暴露出的明确问题，不继续批量改数值、不继续重构异常系统。
4. 路线仍然 1 到 2 张牌就杀穿时，优先回查 `upgrades.ts / contentSelectors.ts / RunEngine.ts` 的路线收益和触发条件。
5. Boss 安全区仍无压力时，优先回查 `battleTemplates.ts / RunEngine.ts` 的安全区尺寸、区外 tick、清弹和 QA 采样。
6. 画面仍乱时，优先回查 `GameScene.ts` 的路线浮字、玩家周围线条、横向车道 overlay、弹道拖尾和 Boss 遮罩。
7. 暂不继续作品集材料整理，暂不新增 Boss / 精英 / 路线。
