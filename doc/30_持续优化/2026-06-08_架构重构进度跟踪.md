# 架构重构进度跟踪

**开始日期**: 2026-06-08  
**配套文档**: `2026-06-08_架构重构与系统策划优化方案.md`  
**提示词文档**: `2026-06-08_架构重构开发提示词.md`

---

## 阶段一：RunEngine 拆分

### 目录结构（已创建）

```
src/systems/
├── RunEngine.ts              # 392KB → 目标 < 500行（待缩减）
├── combat/
│   ├── CombatResolver.ts     # ✅ 已提取 5.8KB - 子弹命中判定、暴击判定、伤害计算
│   ├── BulletSystem.ts       # ✅ 已提取 1.3KB - 子弹移动和生命周期管理
│   ├── DashSystem.ts         # ✅ 已提取 5.2KB - Dash触发逻辑、脉冲伤害、敌人标记
│   ├── DamageCalculator.ts   # ✅ 已提取 2.7KB - 伤害计算纯函数包装
│   └── index.ts              # ✅ 导出
├── spawn/
│   ├── EnemySpawner.ts       # ✅ 已提取 12KB - 敌人生成、archetype选择、位置计算
│   ├── SpawnPatternEngine.ts # ✅ 已提取 6.5KB - 压力模式生成逻辑
│   ├── PressureCurve.ts      # ✅ 已提取 2.1KB - 敌人HP/速度/间隔/数量上限计算
│   └── index.ts              # ✅ 导出
├── route/
│   ├── RouteManager.ts       # ✅ 已提取 1.2KB - 路线管理统一入口
│   ├── IRoutePassive.ts      # ✅ 已提取 1.1KB - 路线被动接口定义
│   ├── CritRoutePassive.ts   # ✅ 已提取 2.8KB - Crit路线被动逻辑
│   ├── PierceRoutePassive.ts # ✅ 已提取 2.4KB - Pierce路线被动逻辑
│   ├── DashRoutePassive.ts   # ✅ 已提取 1.5KB - Dash路线被动逻辑
│   └── RouteProgression.ts   # ✅ 已提取 6.2KB - 路线阶段推进、叙事文本
├── progression/
│   ├── UpgradeEngine.ts      # ✅ 已提取 1.8KB - 升级应用逻辑
│   ├── RouteProgression.ts   # 占位
│   ├── KillStreakSystem.ts   # ✅ 已提取 4.1KB - 连杀/拾取流/击杀流/穿透流
│   └── ExperienceSystem.ts   # ✅ 已提取 6.8KB - 经验球拾取、磁铁吸引、升级触发
├── ai/
│   ├── EnemyAI.ts            # ✅ 已提取 14KB - 敌人AI行为、投射物更新、碰撞检测
│   ├── EliteBehavior.ts      # ✅ 已提取 12KB - 精英敌人行为、压力脉冲、护卫同步
│   └── BossBehavior.ts       # ✅ 已提取 3.2KB - Boss火战线监控、安全区惩罚
└── state/
    ├── BattleStateFactory.ts # ✅ 已提取 5.5KB - BattleState初始化
    ├── RunStateFactory.ts    # ✅ 已提取 1.1KB - RunState初始化
    └── index.ts              # ✅ 导出
```

### 已提取文件详情

#### spawn/PressureCurve.ts (2.1KB)
**作用**: 封装敌人生成相关的纯计算函数，将 RunEngine 中的 HP/速度/间隔/数量上限计算抽离为独立模块。
**从 RunEngine 提取的函数**:
- `getRegularEnemyHp()` → 调用 `balance.getEnemyHealth()`
- `getRegularEnemySpeed()` → 调用 `balance.getEnemyMoveSpeed()`
- `getEnemySpawnIntervalForBattle()` → 计算生成间隔（含压力阶段修正）
- `getRegularEnemyCapForBattle()` → 计算敌人数量上限（含精英修正）

**RunEngine 中对应方法**（待改为调用 PressureCurve）:
- `getRegularEnemyHp()` (line 10060)
- `getRegularEnemySpeed()` (line 10070)
- `getEnemySpawnInterval()` (line 10080)
- `getRegularEnemyCap()` (line 10098)

#### combat/DamageCalculator.ts (2.7KB)
**作用**: 封装所有伤害计算相关的纯函数，这些函数只依赖 PlayerStats/BattleState/RouteBuildStage，不依赖 RunEngine 内部状态。
**从 RunEngine 提取/包装的函数**:
- `calculateCritOverdriveDuration()` → `balance.getCritOverdriveDurationGain()`
- `calculateEffectiveFireRate()` → `balance.getEffectiveFireRate()`
- `calculateEffectiveCritChance()` → `balance.getEffectiveCritChance()`
- `calculateCritSplashRatio()` → `balance.getCritSplashRatio()`
- `calculateDashPulseDamage()` → `balance.getDashPulseDamage()`
- `calculateDashPulseRadius()` → `balance.getDashPulseRadius()`
- `calculateDashDriveDuration()` → `balance.getDashDriveDuration()`
- `calculateDashCooldownAfterPulse()` → `balance.getDashCooldownAfterPulse()`
- `calculateDashPulseHeal()` → `balance.getDashPulseHeal()`
- `calculateDashDamageMultiplier()` → `balance.getDashDamageMultiplier()`
- `calculatePierceEchoCount()` → `balance.getPierceEchoCount()`
- `calculatePierceEchoDamageRatio()` → `balance.getPierceEchoDamageRatio()`
- `calculatePierceCooldownRefund()` → `balance.getPierceCooldownRefund()`

**RunEngine 中对应方法**（待改为调用 DamageCalculator）:
- `getCritOverdriveDurationGain()` (line 10136)
- `getEffectiveFireRate()` (line 10141)
- `getEffectiveCritChance()` (line 10146)
- `getCritSplashRatio()` (line 10151)

#### state/BattleStateFactory.ts (5.5KB)
**作用**: 封装 BattleState 对象的创建逻辑，将 enterBattle 中 150+ 行的状态初始化代码抽离为独立工厂函数。
**从 RunEngine 提取的代码**:
- `enterBattle()` 中的 `this.state.battle = { ... }` 初始化块（line 1015-1165）
- 所有路线被动状态字段的默认值设置
- 所有战斗计时器的初始值设置

**RunEngine 中对应代码**（待改为调用 createBattleState）:
- `enterBattle()` 方法 (line 1003-1168)

#### spawn/EnemySpawner.ts (12KB)
**作用**: 封装所有敌人生成相关逻辑，包括自适应爆发数量、 archetype 选择、生成位置计算、敌人创建、护卫生成。
**从 RunEngine 提取的函数**:
- `getAdaptiveSpawnBurstCount()` → 根据当前敌人构成和模板模式调整生成爆发数量
- `getAdaptiveSpawnArchetype()` → 根据模板偏好和当前敌人构成选择 archetype
- `getSpawnPosition()` → 根据生成模式（surround/pincers/lanes）计算生成位置
- `createArchetypedEnemy()` → 创建单个敌人，应用 archetype 修正
- `spawnEliteSupportEnemies()` → 生成精英护卫，根据精英行为调整位置

**RunEngine 中对应方法**（待改为调用 EnemySpawner）:
- `getAdaptiveSpawnBurstCount()` (line 4778)
- `getAdaptiveSpawnArchetype()` (line 4829)
- `getSpawnPosition()` (line 4868)
- `createArchetypedEnemy()` (line 4971)
- `spawnEliteSupportEnemies()` (line 5046)
- `spawnEnemies()` 中的生成循环部分 (line 4106-4150)

#### progression/KillStreakSystem.ts (4.1KB)
**作用**: 封装连杀计数、拾取流、击杀流、穿透流的计算和更新逻辑。
**从 RunEngine 提取的函数**:
- `registerKillFlow()` → 击杀时更新连杀计数和连杀倍数
- `getPickupFlowWindowSec()` → 根据连杀数计算拾取流窗口时长
- `getPickupFlowRatio()` → 计算拾取流强度比率
- `getKillFlowRatio()` → 计算击杀流强度比率
- `registerPierceFlow()` → 穿透流累积（含 laneScore/echoCount/eliteCrackRatio 加权）
- `registerPickupFlow()` → 拾取流累积
- `getPickupFlowCarry()` / `getKillFlowCarry()` → 计算流携带加成

**RunEngine 中对应方法**（待改为调用 KillStreakSystem）:
- `registerKillFlow()` (line 4318)
- `getPickupFlowWindowSec()` (line 4354)
- `getPickupFlowRatio()` (line 4367)
- `getKillFlowRatio()` (line 4375)
- `registerPierceFlow()` (line 4387)

#### progression/ExperienceSystem.ts (6.8KB)
**作用**: 封装经验球拾取、磁铁吸引、升级触发逻辑。
**从 RunEngine 提取的函数**:
- `gainExperience()` → 经验增加、升级判定、升级面板延迟
- `openQueuedLevelUpPanel()` → 打开排队升级面板
- `updateExperienceOrbs()` → 经验球移动、拾取、连锁真空效应

**RunEngine 中对应方法**（待改为调用 ExperienceSystem）:
- `updateExperienceOrbs()` (line 7375)
- `gainExperience()` (line 7521)
- `openQueuedLevelUpPanel()` (line 7555)

#### route/RouteProgression.ts (6.2KB)
**作用**: 封装路线阶段推进、路线状态判定、路线叙事文本生成。
**从 RunEngine 提取的函数**:
- `advanceRoute()` → 路线推进（hinted→committed→matured）
- `getRouteBuildStage()` → 获取路线当前阶段
- `getBuildStageLabel()` → 阶段标签文本
- `getRouteStageLabel()` → 路线阶段标签（如"一串串炸开"）
- `getRouteStageNarrative()` → 路线阶段叙事文本
- `getRouteStageMomentText()` → 路线阶段 moment 提示文本

**RunEngine 中对应方法**（待改为调用 RouteProgression）:
- `advanceRoute()` (line 3565)
- `getRouteBuildStage()` (line 978)
- `getBuildStage()` (line 2428)
- `getBuildStageLabel()` (line 125)
- `getRouteStageLabel()` (line 3091)
- `getRouteStageNarrative()` (line 3050)
- `getRouteStageMomentText()` (line 3179)

#### spawn/SpawnPatternEngine.ts (6.5KB)
**作用**: 封装压力模式生成逻辑，包括安全区开启、墙壁射击、口袋射击、护卫波次生成。
**从 RunEngine 提取的函数**:
- `executePressurePattern()` → 执行压力模式（laneCrush/sideClamp/crossfireWave）
- `spawnPressureWallShots()` → 生成墙壁射击
- `spawnPressurePocketShots()` → 生成口袋射击

**RunEngine 中对应方法**（待改为调用 SpawnPatternEngine）:
- `executePressurePattern()` (line 1322)
- `spawnPressureWallShots()` (line 1967)
- `spawnPressurePocketShots()` (line 2017)

#### route/IRoutePassive.ts (1.1KB)
**作用**: 定义路线被动接口，包括 HitContext/KillContext/DashContext 等上下文类型和 IRoutePassive 接口。

#### route/CritRoutePassive.ts (2.8KB)
**作用**: 封装 Crit 路线被动逻辑，包括破绽累积、终结打击、爆发连锁、焦点锁定。
**从 RunEngine 提取的逻辑**:
- `updatePassiveTimers()` → critComboDecaySec/critBurstChainSec/critFocusLockSec 递减
- `applyCritPassiveOnHit()` → 暴击命中时的破绽累积和终结打击
- `applyCritPassiveOnKill()` → 暴击击杀时的 critOverdriveSec 延长

**RunEngine 中对应代码**（待改为调用 CritRoutePassive）:
- `updateBullets()` 中的 crit 被动部分 (line 5379-5406)
- `handleEnemyDefeated()` 中的 crit 部分 (line 8022-8046)

#### route/PierceRoutePassive.ts (2.4KB)
**作用**: 封装 Pierce 路线被动逻辑，包括穿透印记、连锁累积、连锁爆发、裂纹扩散。
**从 RunEngine 提取的逻辑**:
- `updatePassiveTimers()` → pierceChainDecaySec 递减
- `applyPiercePassiveOnHit()` → 穿透命中时的印记累积和连锁爆发
- `applyPiercePassiveOnKill()` → 穿透击杀时的印记传播

**RunEngine 中对应代码**（待改为调用 PierceRoutePassive）:
- `updateBullets()` 中的 pierce 被动部分 (line 5451-5481)
- `handleEnemyDefeated()` 中的 pierce 部分 (line 8302-8333)

#### route/DashRoutePassive.ts (1.5KB)
**作用**: 封装 Dash 路线被动逻辑，包括幽灵打击、动量累积、反击窗口。
**从 RunEngine 提取的逻辑**:
- `updatePassiveTimers()` → dashMomentumDecaySec/dashCounterWindowSec 递减
- `applyDashPassiveOnDash()` → Dash 触发时的幽灵打击就绪和动量累积
- `applyDashPassiveOnHit()` → Dash 命中时的回切窗口收益

**RunEngine 中对应代码**（待改为调用 DashRoutePassive）:
- `updatePlayerMovement()` 中的 dash 被动部分 (line 3870-3878)
- `updateBullets()` 中的 dash 被动部分 (line 5876-5894)

#### route/RouteManager.ts (1.2KB)
**作用**: 路线管理统一入口，聚合三个 RoutePassive，提供统一的计时器更新和状态查询。

#### progression/UpgradeEngine.ts (1.8KB)
**作用**: 封装升级应用逻辑，包括效果应用、路线推进、分支切换记录。
**从 RunEngine 提取的函数**:
- `applyEffects()` → 应用升级/事件效果（stats/heal/route）

**RunEngine 中对应方法**（待改为调用 UpgradeEngine）:
- `applyEffects()` (line 3525-3563)

#### state/RunStateFactory.ts (1.1KB)
**作用**: 封装 RunState 对象的创建逻辑。
**从 RunEngine 提取的代码**:
- `constructor()` 中的 `this.state = { ... }` 初始化块（line 197-234）

**RunEngine 中对应代码**（待改为调用 createRunState）:
- `constructor()` 方法 (line 194-236)

#### combat/BulletSystem.ts (1.3KB)
**作用**: 封装子弹移动和生命周期管理，包括子弹位置更新、生命周期检查、边界过滤。
**从 RunEngine 提取的逻辑**:
- `updateBullets()` 中的子弹移动部分 → bullet.x/y += bullet.vx/vy * dt
- 子弹生命周期检查 → bullet.lifeSec -= dt
- 边界过滤 → 移除超出 Arena 范围的子弹

#### combat/CombatResolver.ts (5.8KB)
**作用**: 封装子弹命中判定、暴击判定、伤害计算和应用逻辑。
**从 RunEngine 提取的逻辑**:
- 暴击判定 → Math.random() < getEffectiveCritChance()
- 伤害计算 → critMultiplier、recoveryRatio、eliteCrackRatio 等加成
- 命中效果 → registerEnemyImpact、kickBattleShake、createCombatPulse

#### combat/DashSystem.ts (5.2KB)
**作用**: 封装 Dash 触发逻辑，包括冷却管理、脉冲伤害、敌人标记、回切窗口。
**从 RunEngine 提取的逻辑**:
- Dash 触发条件 → dashCooldownSec <= 0 && (dashPulseDamage > 0 || routeCounts.dash > 0)
- 脉冲伤害应用 → 对范围内敌人造成 pulseDamage
- 敌人标记 → dashMarkSec、dashPulseStacks
- 回切窗口 → dashCounterWindowSec = 1.2

#### ai/EnemyAI.ts (14KB)
**作用**: 封装敌人 AI 行为，包括敌人更新、敌人投射物更新、碰撞检测、Dash 擦过检测。
**从 RunEngine 提取的逻辑**:
- `updateEnemies()` → 敌人移动、碰撞检测、Dash 擦过
- `updateEnemyProjectiles()` → 敌人投射物移动、玩家命中检测
- 各种 archetype 敌人 AI → standard/brute/skirmisher/ranged

#### ai/EliteBehavior.ts (12KB)
**作用**: 封装精英敌人行为，包括压力脉冲、护卫同步、裂口机制、追击行为。
**从 RunEngine 提取的逻辑**:
- `updateEliteEnemy()` → 精英移动逻辑
- `startElitePressurePulse()` → 压力脉冲触发
- 各种行为模式 → kiting/screened/summoner/frontline

#### ai/BossBehavior.ts (3.2KB)
**作用**: 封装 Boss 行为，包括火战线监控、安全区惩罚、阶段转换。
**从 RunEngine 提取的逻辑**:
- `updateBossFirelineMonitoring()` → Boss 火战线覆盖率计算
- `applyBossSafeWindowPenalty()` → 安全区外伤害

### 阶段一：GameScene 拆分（待开始）

```
src/scenes/
├── GameScene.ts              # 212KB → 目标 < 800行
├── renderers/
│   ├── BattleRenderer.ts     # 待提取
│   ├── EffectRenderer.ts     # 待提取
│   ├── UIRenderer.ts         # 待提取
│   └── CameraController.ts   # 待提取
├── input/
│   ├── InputHandler.ts       # 待提取
│   ├── KeyboardInput.ts      # 待提取
│   └── TouchInput.ts         # 待提取
└── debug/
    └── DebugOverlay.ts       # 待提取
```

---

## 阶段二：接口抽象与事件系统（未开始）
## 阶段三：数据驱动改造（未开始）
## 阶段四：测试补全（未开始）
## 阶段五：文档完善（未开始）

---

## 构建验证记录

| 时间 | 操作 | 结果 |
|------|------|------|
| 2026-06-08 14:06 | 创建目录结构 + 3个提取文件 | ✅ 通过 |
| 2026-06-08 14:15 | 提取 EnemySpawner + KillStreakSystem + ExperienceSystem + RouteProgression | ✅ 通过 |
| 2026-06-08 14:25 | 提取 SpawnPatternEngine + IRoutePassive + CritRoutePassive + PierceRoutePassive + DashRoutePassive + RouteManager + UpgradeEngine + RunStateFactory | ✅ 通过 |
| 2026-06-08 14:35 | 提取 BulletSystem + CombatResolver + DashSystem + EnemyAI + EliteBehavior + BossBehavior | ✅ 通过 |
| 2026-06-08 14:45 | 集成子系统到 RunEngine（添加导入和实例化） | ✅ 通过 |

### 已提取代码统计

| 子系统 | 文件数 | 总大小 | 提取内容 |
|--------|--------|--------|----------|
| spawn | 3 | 20.6KB | PressureCurve + EnemySpawner + SpawnPatternEngine |
| combat | 4 | 15KB | DamageCalculator + BulletSystem + CombatResolver + DashSystem |
| progression | 3 | 12.7KB | KillStreakSystem + ExperienceSystem + UpgradeEngine |
| route | 6 | 15.2KB | RouteProgression + IRoutePassive + CritRoutePassive + PierceRoutePassive + DashRoutePassive + RouteManager |
| ai | 3 | 29.2KB | EnemyAI + EliteBehavior + BossBehavior |
| state | 2 | 6.6KB | BattleStateFactory + RunStateFactory |
| **合计** | **21** | **99.3KB** | 从 RunEngine 392KB 中已提取 99.3KB (25.3%) |

### 阶段一完成状态

- [x] 创建子系统目录结构
- [x] 提取 spawn 子系统 (PressureCurve + EnemySpawner + SpawnPatternEngine)
- [x] 提取 combat 子系统 (DamageCalculator + BulletSystem + CombatResolver + DashSystem)
- [x] 提取 route 子系统 (RouteProgression + IRoutePassive + CritRoutePassive + PierceRoutePassive + DashRoutePassive + RouteManager)
- [x] 提取 progression 子系统 (KillStreakSystem + ExperienceSystem + UpgradeEngine)
- [x] 提取 ai 子系统 (EnemyAI + EliteBehavior + BossBehavior)
- [x] 提取 state 子系统 (BattleStateFactory + RunStateFactory)
- [x] 添加子系统导入和实例化
- [ ] 将 RunEngine 中的代码改为调用提取的子系统（待后续迭代）
- [ ] RunEngine.ts 缩减到 < 500行（待后续迭代）
