# 当前阶段开发总表

## 文档状态

- 状态：`当前生效 / 验收后持续更新`
- 更新时间：`2026-06-17（P2-0 收口完成）`
- 适用阶段：`P2-0 验证链已修复，stage doc 已同步，P2-0 收口完成`
- 用途：作为当前阶段唯一的开发总表。后续每轮验收完成后，优先更新这份文档，不再把"当前还要做什么"散落在聊天记录和旧待办里。

---

## 当前结论

当前仓库已经不再停留在 `P1-1 / P1-2` 的状态。

代码主线实际上已经继续往前走，进入了更偏 `showcase / 可控展示` 的阶段，至少包含：

1. 路线升级分发已经出现明确的 `showcase` 入口和定向池
2. 路线阶段展示口径已经被收成更偏展示态的短句
3. 最近几轮提交也已经在围绕 `showcase route flow` 和文案终稿做收口

但文档和 QA 没有同步跟上，导致当前真实状态是：

1. 代码已经往展示态走了
2. 阶段文档还停在"P1 继续推进自然流程"
3. 固定验证链已经被当前结果页结构打断
4. 所以下一轮主线不该再回到旧的 `P0 / P1` 打磨，而是先修复验证链并同步阶段状态

当前最准确的判断是：

1. `P1-1：自然流程整体可玩性回归` 已经不再是当前最高优先级主线
2. `P1-2：玩家文案终稿专项` 视为已完成
3. `P2-0：showcase 验证链修复与阶段文档同步` 已收口完成
4. 固定验证流程已全绿，下一轮可进入 `P2-1：展示整理与面试材料`

---

## 当前已知阻塞

### 1. stable smoke 已被当前结果页协议打断

当前按固定流程执行时：

1. `npm run export:data` 通过
2. `npm run build` 通过
3. `npm run preview -- --host 127.0.0.1 --port 4173` 通过
4. `npm run qa:stable-smoke` 通过

已知失败点：

- ~~`tools/qa-stable-smoke.mjs` 仍在等待 `[data-action="details"]`~~（已修复：脚本已适配当前结果页结构，仅依赖 `restart / menu`）
- 当前结果页主操作只保留了 `restart / menu`（已适配）

**当前状态：P2-0 已修复，固定验证链已闭环。**

### 2. 当前磁盘上的旧 QA 产物不能直接代表 HEAD

仓库里仍有较早的：

- `output/qa/stable-smoke-current/summary.json`
- `output/qa/smart-natural-fullrun-current/summary.json`

但这些产物不能自动视为当前 HEAD 的有效验收结论。

原因很简单：

1. 最近已经继续合入 showcase 相关提交
2. 当前 stable smoke 在 HEAD 上已经能复现失败
3. 所以下一轮必须先产出新一轮、对得上当前代码的 QA 结论

---

## 当前阶段划分

### P1：上一阶段收口结论

#### P1-1：自然流程整体可玩性回归

当前状态：

- `已完成主线使命，不再作为当前主攻方向`

结论：

1. 这条线已经把 `routeId=none / buildStage=unknown / timedOutRuns` 从更差的状态往前推过几轮
2. 后续即便还有自然流程回归，也只作为 showcase 阶段的回归项，不再单独开回 `P1-1`
3. 当前问题已经不是"继续压一轮自然流指标"，而是"当前 showcase 代码能否重新走通固定验收链"

#### P1-2：玩家文案终稿专项

当前状态：

- `已完成`

结论：

1. 高频玩家可见文本已经完成一轮较完整的清扫
2. 当前不再建议重新开启文案专项
3. 后续只接受与 showcase 验收直接相关的小回归，不回到大面积文案修边

---

### P2：展示整理与面试准备

这部分已经不是"未开始，不插队"。

当前代码其实已经先走到这里了，但 QA 和文档没同步，所以要拆成两个阶段看：

#### P2-0：showcase 验证链修复与阶段文档同步

当前状态：

- `已完成`

本轮成果（2026-06-17）：

1. 验证工具适配：`tools/qa-stable-smoke.mjs` 已确认适配当前结果页结构，仅依赖 `[data-action="restart"]` 和 `[data-action="menu"]`，不需要旧 `details` 按钮。结果页详图链路已基于 showcase 结果页结构闭环。
2. 固定验证流程重新全绿通过：
   - `npm run export:data` ✅
   - `npm run build` ✅
   - `npm run preview -- --host 127.0.0.1 --port 4173` ✅
   - `npm run qa:stable-smoke` ✅
   - `node tools/qa-smart-natural-fullrun.mjs` ✅
3. 新 QA 产物已输出：
   - `failed404Urls: []`
   - `consoleErrors: []`
   - `consoleWarns: []`
   - `output/qa/stable-smoke-current/summary.json` 已刷新
   - `output/qa/smart-natural-fullrun-p2-0/summary.json` 可读可引用
4. 文档已同步到当前 showcase / P2-0 口径：
   - `当前阶段开发总表.md` 已更新
   - `progress.md` 乱码已修复，结论已同步
   - `测试与验证手册.md` 确认固定流程无需修改
5. natural fullrun（5 轮）产出：
   - `consoleErrorRuns: 0`
   - `failed404Urls: []`
   - `bossReachedRuns: 5/5`
   - `wins: 2/5`（40%，含 dash/pierce/crit 路线胜利）
   - `timedOutRuns: 2/5`（Boss 战自然超时）
   - `buildStage=matured: 3/5`

验收结论：

所有验收标准已通过（详见 `output/qa/stable-smoke-current/summary.json` 和 `output/qa/smart-natural-fullrun-p2-0/summary.json`）。

- ✅ `npm run build` 通过
- ✅ `npm run qa:stable-smoke` 通过（`failed404Urls: []`、`consoleErrors: []`、`consoleWarns: []`）
- ✅ natural fullrun 有可读的新 `summary.json`
- ✅ `progress.md` 无乱码
- ✅ 文档明确写出当前 P2-0 结论

#### P2-1：展示整理与面试材料

当前状态：

- `待开始，但必须排在 P2-0 之后`

开发目标：

1. 整理当前可展示链路：
   - `crit`
   - `pierce`
   - `dash`
   - `Boss`
2. 固定成功局 / 失败局展示入口
3. 补齐关键截图、短视频、结果页讲法
4. 形成面试可直接讲的"问题 -> 修改 -> 验证 -> 收益"材料

进入条件：

1. `P2-0` 完成
2. 固定验证流程重新稳定
3. 当前 showcase 产物已能被 QA 复现和回收

---

## 当前不建议再开的旧主线

以下方向当前都不建议再单开主线：

1. 继续回到 `crit payoff / Boss signature` 做 P0 式微调
2. 再把 `P0-3` 拉回去继续抠 `late / finalPrep` 权重
3. 再开一轮 `P1-2` 文案专项
4. 重新把主逻辑塞回 `RunEngine.ts`

原因：

1. 这些线要么已经收口，要么边际明显变薄
2. 当前真正卡点是 showcase 验收链断开
3. 先把"当前代码能被稳定验收"修好，后面的展示整理才站得住

---

## 下一轮推荐顺序

1. 先读：
   - `E:\codex\auto-shooter-demo\doc\30_持续优化\当前阶段开发总表.md`
   - `E:\codex\auto-shooter-demo\doc\30_持续优化\测试与验证手册.md`
   - `E:\codex\auto-shooter-demo\progress.md`
2. `P2-0` 已完成，固定验证流程已全绿
3. 下一轮可进入 `P2-1：展示整理与面试材料`
4. 如果继续回归验证，仍按以下顺序跑固定流程：
   - `npm run export:data`
   - `npm run build`
   - `npm run preview -- --host 127.0.0.1 --port 4173`
   - `npm run qa:stable-smoke`
   - `node tools/qa-smart-natural-fullrun.mjs`

---

## 一句话判断

当前不是"继续做旧的玩法微调"，也不是"已经可以直接整理面试材料"。

当前最正确的位置是：

`P2-0 已收口，固定验证流程已全绿；下一轮推荐进入 P2-1：展示整理与面试材料。`
