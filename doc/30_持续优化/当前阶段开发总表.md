# 当前阶段开发总表

## 文档状态

- 状态：`当前生效 / 验收后持续更新`
- 更新时间：`2026-06-12`
- 适用阶段：`P0 微调线结束后，回到作品本体开发`
- 用途：作为当前阶段唯一的开发总表。后续每轮验收完成后，优先更新这份文档，不再把“当前还要做什么”散落在聊天记录和旧待办里。

---

## 当前结论

当前项目已经完成了主流程闭环、玩家可见文本清扫、选择页/结果页可读性收口、`crit / pierce` 样板链、Boss 开招可读性第一层，以及 QA 稳定 smoke 基建。

但项目还没有进入“只做展示整理”的阶段。

当前最准确的判断是：

1. `crit payoff / Boss signature` 这条 P0 微调线已经停止继续投入
2. 停止原因不是功能无效，而是边际收益已经明显变薄
3. 接下来应回到“作品本体还没做完的大块开发”，而不是继续抠这一条线
4. 展示整理、面试材料、玩家文案终稿，都排在作品本体收口之后
5. `dash` 主线已经跨过“只靠 QA 样板成立”的阶段，当前不再作为最高优先级专项继续深挖，后续只保留针对 `finisher` 偏弱点的顺手回归
6. 当前下一条最该推进的主线，已经切到 `P1-1：自然流程整体可玩性回归`

当前保留基线：

- 最新有效提交：`84fb8598f7b85ca960f65261982d4f9d549cb6a6`
- 提交信息：`feat: deepen dash mainline split`
- 当前判断：`保留该状态，作为 dash 主线收口基线，并切到 P1-1 自然流程整体可玩性回归`

---

## 已完成的大块

以下内容已经不再是当前主攻方向，后续只做回归或小修：

1. 主流程闭环
   - 首页、战斗、升级、异常、结果页、详情页已经接通

2. 玩家可见文本第一轮清扫
   - 系统腔、设计腔、表头味、说明味已大幅压下
   - QA / stable smoke 页面也已纳入玩家文本规则

3. 选择页 / 异常页 / 结果页可读性收口
   - 信息层级、密度和主操作落点已比早期稳定很多

4. `crit / pierce` 样板闭环
   - 异常页 -> battle bridge/payoff -> result detail 已可稳定验收

5. Boss 开招可读性第一层
   - 阶段切换不再只是阶段名，已经能看到 signature / pattern 级提示

6. QA 稳定验收入口
   - 已有稳定 smoke 可覆盖首页、升级、异常、战斗片段、结果页

---

## 当前主线

### P0：作品本体完成度

这不是上一条 `crit payoff / Boss signature` 微调线，而是回到仍未做完的核心开发。

#### P0-1：路线体系完整度

当前问题：

- `crit / pierce` 已有展示样板，但不等于整套路线体系已经做完
- 路线从 starter -> bridge -> payoff -> finisher 的完整感还不统一
- 中段与后段仍有部分内容更像“数值变大”，不是“玩法身份更清楚”

开发目标：

1. 让三条路线都拥有完整、可讲清的成长链
2. 让中段和后段内容不只是补数值，而是真正改变战斗处理方式
3. 让结果页复盘能对得上实战体验，而不是只对得上 QA 样板

当前状态：

- `crit`：已有 bridge / payoff 记忆点，但中段仍偏弱
- `pierce`：样板最完整，但正式内容层仍可继续补厚
- `dash`：已经进入本阶段主线开发，当前在补 starter / bridge / payoff 的闭环

建议关注文件：

- `E:\codex\auto-shooter-demo\src\data\upgrades.ts`
- `E:\codex\auto-shooter-demo\src\data\events.ts`
- `E:\codex\auto-shooter-demo\src\data\routes.ts`
- `E:\codex\auto-shooter-demo\src\systems\RunEngine.ts`
- `E:\codex\auto-shooter-demo\src\scenes\GameScene.ts`

#### P0-2：`dash` 主线补齐

当前问题：

- `dash` 一直被刻意放在回归位，没有进入这轮展示主线
- 如果作品本体要完整，`dash` 不能长期停留在“能跑、但没专项打透”

开发目标：

1. 给 `dash` 一条和 `crit / pierce` 同等级的完整成长链
2. 明确它的战斗身份、强势时刻、处理问题方式
3. 补齐它在异常、战斗反馈、结果页复盘中的可读性

当前状态：

- `已完成`
- `dash` 已经从 QA 样板闭环推进到真实战斗样本和 natural fullrun 样本，当前不再停留在“能演示，但还没真实成立”的状态。
- 这一轮收口后，`DashRoutePassive` / `DashSystem` / `RouteProgression` 已经成为源头模块，`RunEngine` 只留必要接线，结构方向符合当前拆分规则。
- 当前残留问题已经收缩到 `finisher` 偏弱，不再需要继续按最高优先级专项推进；后续只做顺手回归。

建议关注文件：

- `E:\codex\auto-shooter-demo\src\data\upgrades.ts`
- `E:\codex\auto-shooter-demo\src\data\events.ts`
- `E:\codex\auto-shooter-demo\src\systems\RunEngine.ts`
- `E:\codex\auto-shooter-demo\src\scenes\GameScene.ts`
- `E:\codex\auto-shooter-demo\src\ui\OverlayController.ts`

#### P0-3：内容层完整度

当前问题：

- 现在的玩法结构已经能跑，但内容厚度还不够像“完整作品”
- 节点、异常、升级、路线中段与后段的内容，还能继续补层次和差异
- 自然流程的体验不应只依赖少数样板局成立

当前状态：

- 进行中
- 本轮已把 `battleTemplates / upgrades` 的新增内容同步到运行时 JSON，stable smoke 仍稳
- 本轮继续把 `contentSelectors` 的阶段偏置和 `nodes / events / upgrades` 的阶段权重往 opening / mid / late 收紧了一档，固定验证顺序也已经统一成 `npm run export:data -> npm run build -> npm run qa:stable-smoke -> natural fullrun`
- 真实对局里已经能命中新节点，但自然 fullrun 目前还偏少量样本命中，说明内容层差异还需要继续收
- 2026-06-12 这一轮把 late / finalPrep 再给了一次边际验证机会：真实流程里能看到 `round-3-upgrade-commit-hold -> final-prep -> final-boss-bastion` 和 `round-3-upgrade-rareline -> final-prep-shadow -> final-boss-bastion`，但 6 局里只有 2 局到 Boss、0 局通关，收益仍偏温和。

开发目标：

1. 补足节点、异常、升级的内容层次
2. 让不同节点真的像不同考题，不只是不同名字
3. 让玩家能更稳定地感受到“这把是怎么长起来的”

当前状态：

- `进行中`
- `dash` 主线收口后，这一项已经成为当前最高优先级主线。
- 重点不再是继续抠单一路线的样板图，而是补齐 `nodes / events / upgrades / battleTemplates / contentSelectors` 的自然流程层次，让不同局真的长得不一样。
- `crit / pierce / dash / Boss` 在这一阶段只做回归，不再各自单开精修线。
- 本轮验证里，稳定 smoke 继续干净；单局 natural fullrun 已经能自然跑到 `round-1-event`、`round-2-battle-crit-pressure`、`round-3-battle-dash-soft-closeout`，但还没到每局都稳定覆盖多条新内容的程度。
- 最新判断：P0-3 已经接近收口。late / finalPrep 的职责比上一轮更清楚，但 natural fullrun 的整体收益没有明显再抬一档；后续若继续做，应该只做很小的内容补洞或转入 P1 自然流程可玩性回归，不建议继续用多轮 P0-3 拉扯。

建议关注文件：

- `E:\codex\auto-shooter-demo\src\data\nodes.ts`
- `E:\codex\auto-shooter-demo\src\data\events.ts`
- `E:\codex\auto-shooter-demo\src\data\upgrades.ts`
- `E:\codex\auto-shooter-demo\src\data\battleTemplates.ts`
- `E:\codex\auto-shooter-demo\src\data\contentSelectors.ts`

---

### P1：完成度与可玩性收口

#### P1-1：自然流程整体可玩性回归

当前状态：

- `进行中`
- This round pushed opening / mid stability one step forward, but it still has not reached the point where most runs naturally make it to round-3.
- Next, keep pressure on `routeId=none`, `buildStage=unknown`, and `timedOutRuns`.

当前问题：

- 上一条 P0 微调线已经停止，不代表自然流程整体已经定型
- 后续在做路线和内容结构时，仍要顺手继续看自然流程是否更顺
- 这里不再开单独的 `crit / Boss` 专项，而是跟随本体开发一起回归

开发目标：

1. 确保新增内容没有把自然流程带坏
2. 确保 build 的形成和兑现不只存在于 QA 样板
3. 确保 `smart / natural / stable smoke` 三条验证链继续干净

验收要求：

- `npm run build` 通过
- `failed404Urls: []`
- `consoleErrors: []`
- `consoleWarns: []`

Recent validation:

- Ran `npm run export:data -> npm run build -> npm run qa:stable-smoke -> natural fullrun` in the fixed order; stable smoke stayed clean.
- `natural fullrun` improved versus the previous sample: `timedOutRuns` dropped from 4/6 to 3/6, `avgNodesCleared` rose from 1.67 to 2.0, and `bossReachedRuns` reached 5/6.
- `routeCounts.none` and `buildStageCounts.unknown` are still 3/6, so P1-1 still has one more small step left.

#### P1-2：玩家文案终稿专项

当前问题：

- 仍然存在少量“太长、太像 AI、太像指导句、太像设计批注”的文本
- 这类问题已经记录，但不适合继续零碎插在玩法轮里打断主线

开发目标：

1. 简洁
2. 去 AI 味
3. 去指导味
4. 像正常游戏内文本

当前状态：

- `已记录，后置处理`

说明：

- 你点名的 `crit-reroute-window-transform` 这类问题，属于这个专项处理范围

建议关注文件：

- `E:\codex\auto-shooter-demo\src\data\events.ts`
- `E:\codex\auto-shooter-demo\src\data\upgrades.ts`
- `E:\codex\auto-shooter-demo\src\systems\RunEngine.ts`
- `E:\codex\auto-shooter-demo\src\ui\OverlayController.ts`
- `E:\codex\auto-shooter-demo\doc\10_设计文档\玩家可见文本规范.md`

---

### P2：展示整理与面试材料

这部分不是当前下一步，而是作品本体收口后的下一阶段。

包括：

1. 成功局 / 失败局展示链整理
2. `crit / pierce / dash / Boss` 关键截图与短视频
3. 项目讲法、亮点页、案例页
4. 设计问题 -> 修改方案 -> 验证结果 的展示材料整理

当前状态：

- `未开始，不插队`

---

## 已结束的专项

### 已结束：`crit payoff / Boss signature` P0 微调线

结束原因：

1. 最近一轮判定已确认新增真实收益不足
2. 主要收益开始回到 QA 样板和 directed sample
3. 边际收益明显变薄
4. 不符合“先完成整个作品”的阶段目标

后续规则：

- 不再为这条线单开专项
- 只做回归
- 若后续本体开发顺手带来收益，记为附带改进，不重新立项

---

## 暂不处理的内容

以下内容当前不作为下一轮优先项：

1. 继续深抠 `crit payoff` 自然成立率
2. 继续深抠 Boss signature 真实 fullrun 可见性
3. 再扩 stable smoke 基建
4. 重新开全局文本清扫
5. 直接进入展示整理

---

## 每轮验收后的更新规则

后续每轮验收完成后，按下面规则更新本文件：

1. 更新“更新时间”
2. 在对应条目后补状态：
   - `未开始`
   - `进行中`
   - `已完成`
   - `已停止`
   - `后置`
3. 若优先级变化，直接调整 P0 / P1 / P2 顺序
4. 若某条线边际收益明显变薄，明确写入“已结束的专项”
5. 在文末追加一段“最近一次验收结论”

不要做的事：

- 不再把当前阶段主线只写在聊天记录里
- 不再同时维护多份“当前待办”口径
- 不要把 QA 样板收益误写成真实流程收益

---

## 最近一次验收结论

### 2026-06-10

结论：

- `dash` 这条线可以收口了。它已经不再只靠 stable smoke 成立，而是同时拿到了真实战斗样本和 natural fullrun 的成立证据。
- 这轮最有价值的地方不是再抠文案，而是把 `dash` 的主收益继续留在拆分后的源头模块里，没有让 `RunEngine` 回头重新变胖。
- 当前最弱点已经收缩成 `finisher` 偏弱，这属于后续顺手回归项，不再值得继续按 P0-2 的最高优先级多压几轮。

当前下一步：

- 把主线切到 `P0-3：内容层完整度`，继续补 `nodes / events / upgrades / battleTemplates / contentSelectors` 的自然流程差异和内容厚度，同时保住 `crit / pierce / dash / Boss` 回归稳定。

### 2026-06-10

结论：

- `dash` 已经进入本体主线，当前不再停留在纯回归位
- 这轮先收口了几处不够自然的 dash 短句，让展示文案和战斗阶段说法更一致
- build 通过，当前没有看到编译级拆分问题

当前下一步：

- 继续做 dash 的主线补齐，同时保住 `crit / pierce / Boss` 的回归稳定性

### 2026-06-10

结论：

- 这轮把 `dash` 的源头逻辑继续往拆分模块里收，`DashRoutePassive` / `DashSystem` / `RouteProgression` 现在是主入口，`RunEngine` 只留接线和少量兼容段。
- `dash` 的真实战斗证据比上一轮更完整了，至少已经能从命中残影、贴身回打提示和自然结果页里看出这条线在往前走。
- `stable smoke`、真实战斗样本和自然 fullrun 样本都还在，`crit / pierce / boss` 回归没有被这轮带坏。

当前下一步：

- 继续看 `dash` 的 starter / bridge / payoff 还能不能再往前推一刀，重点盯真实战斗里的 finisher 还差多少。

### 2026-06-09

结论：

- `crit payoff / Boss signature` 这条 P0 微调线到此为止
- 原因不是无效，而是边际收益已不足以继续投入
- 当前阶段应转回作品本体开发

验收依据：

1. 本轮无源码改动
2. stable smoke 仍稳定
3. natural fullrun 没有拿出比上一轮更硬的真实流程证据
4. Boss 仍更依赖 directed sample
5. `pierce` 正常，但没有新增边际收益

当前下一步：

- 重新进入“路线体系完整度 + dash 主线补齐 + 内容层完整度”这三块主线开发

### 2026-06-11

结论：

- 内容层这轮已经从“源码里补出来”推进到“运行时也吃到”，`针线压场 / 桥口分层 / 收束缝线` 这批节点和对应模板已经能在真实流程里被抽到
- `npm run export:data` 现在是内容层验证的必要步骤，不再只是 build 之后看源码
- stable smoke 继续干净，`failed404Urls: []`、`consoleErrors: []`、`consoleWarns: []`
- 真实战斗样本已经能打到新节点，但自然 fullrun 仍偏少量样本命中，后续还可以继续抬内容分化的自然出现率

当前下一步：

- 继续推 `P0-3：内容层完整度`，优先把新节点在自然流程里的出现率再抬一档，再顺手补 `progress.md` 里的验证流程说明和这轮收口结论
### 2026-06-11 后续补记
- 这轮继续把内容层往自然流程里推了一档：`contentSelectors` 的阶段偏置和 `nodes / events / upgrades` 的阶段权重又收紧了一次，`npm run export:data -> npm run build -> npm run qa:stable-smoke -> natural fullrun` 现在是固定验证顺序。
- 稳定 smoke 仍然干净；单局自然 fullrun 已经能自然跑到 `round-1-event`、`round-2-battle-crit-pressure`、`round-3-battle-dash-soft-closeout`，并给出真实失败局结果页。
- 这一轮说明内容层已经不是只在 QA 样板里成立，但自然 fullrun 还没有到“每局都稳定覆盖多条新内容”的程度，下一步继续盯命中率和阶段分化。

### 2026-06-12

结论：

- 这轮没有回到单线平衡，也没有把逻辑塞回 `RunEngine.ts`；改动继续留在 `contentSelectors / nodes / events / upgrades` 这些内容层模块里。
- late / finalPrep 的职责更集中：late 更容易先给收束或 Boss 前整备，finalPrep 更偏押注、补短板和最后一手，而不是继续混进太多中段桥接内容。
- stable smoke 继续干净，`failed404Urls: []`、`consoleErrors: []`、`consoleWarns: []`，`crit / pierce / dash / Boss` 回归没有被这轮内容层调整带坏。
- natural fullrun 6 局里有 2 局自然走到 Boss，命中了 `round-3-upgrade-commit-hold -> final-prep -> final-boss-bastion` 和 `round-3-upgrade-rareline -> final-prep-shadow -> final-boss-bastion`；但 0 通关、4 局超时，平均清节点 1.67，说明这轮更像“略微更顺一点”，不是把 P0-3 再明显抬一档。

当前下一步：

- P0-3 建议在这轮后准备收口。后续最值得做的是 P1 自然流程可玩性回归，或者只补极少数 late / finalPrep 内容洞；不建议继续用 P0-3 名义反复压权重和样本。

### 2026-06-12

结论：

- 这轮继续把 late / finalPrep 的节点职责往"收束 / 押注 / Boss前复检"收紧了一次：
  - `nodes.ts` 把 round-3 节点选择顺序改成 `support -> battle -> support`，并新增 `决战复检` 节点
  - `contentSelectors.ts` 把 late / finalPrep 的 tag bonus 和 rare multiplier 又抬了一档，finalPrep 的路线封口池现在更偏 `payoff / finisher`
  - `events.ts` 给 `closeout-echo` / `blackbox-bargain` 补了更强的 dominant / committed 路线亲和，并新增 `boss-sightline` 这个 late 异常
  - `upgrades.ts` 把三张通用 late 卡的 `finalPrepBonus` 和 `finisher` tag 再收紧了一点
- 验证结果：
  - `npm run export:data` 通过
  - `npm run build` 通过
  - 预览地址：`http://127.0.0.1:4173/game-demo/`
  - stable smoke：`E:\codex\auto-shooter-demo\output\qa\stable-smoke-late-finalprep-20260612\summary.json`
    - `failed404Urls: []`
    - `consoleErrors: []`
    - `consoleWarns: []`
    - crit / pierce 回归继续稳定
  - natural fullrun：`E:\codex\auto-shooter-demo\output\qa\smart-natural-fullrun-late-finalprep-20260612\summary.json`
    - 6 局 0 胜，2 局到 Boss，4 局 timeout
    - 到 Boss 的 2 局分别命中了：
      - run1: `round-3-upgrade-commit-hold` -> `final-prep` -> `final-boss-bastion`
      - run4: `round-3-upgrade-rareline` -> `final-prep-shadow` -> `final-boss-bastion`
    - 说明 late / finalPrep 节点在真实流程里确实更容易出现了，但自然样本里仍然只有少量局能稳定走到这些节点

当前判断：

- 这轮给了 P0-3 一次边际验证机会，结果是：late / finalPrep 的自然命中比上一轮更稳定了一点，但整体收益仍属温和增量，而不是明显再抬一档。
- 6 局里只有 2 局真正走到 late / finalPrep，另外 4 局都 timeout 在更早阶段，说明 opening / mid 的自然流程仍然偏薄，单纯继续压 late / finalPrep 权重已经开始边际变薄。
- **P0-3 应该在这轮后收口。** 后续如果还要继续补内容层，建议转向"补 opening / mid 的内容分化和自然命中稳定性"，而不是继续单独盯 late / finalPrep。

下一步建议：

- 如果团队判断 P0-3 还有一轮价值，优先补 opening / mid 的内容厚度和节点多样性，让更多局能稳定走到 round-3。
- 如果团队判断 P0-3 已经接近收口，建议转到 P1-1（自然流程整体可玩性回归）或 P1-2（玩家文案终稿），不再继续开内容层权重调整专项。
