# 2026-04-30 74fa9bd 参数验收与表现层反馈小步补强

## 本轮目标

先验收 74fa9bd 的拾取、经验、第二轮精英权重调整是否有副作用；如果没有明显问题，再小步补强画面读局、打击反馈、音效层次和通俗提示，让游戏更清楚、更顺、更像完整可玩的版本。

## 74fa9bd 参数验收

### 参数改动回顾

**balance.ts**：
- 拾取范围：34 + moveSpeed * 0.045 → 38 + moveSpeed * 0.055（提升约 12%）
- 磁吸范围：132 + moveSpeed * 0.14 → 148 + moveSpeed * 0.16（提升约 12%）
- 经验值：baseValue = 4 + round * 2 + phaseTier * 2 + enemyHp * 0.08 → 5 + round * 2.5 + phaseTier * 2.5 + enemyHp * 0.09（提升约 15%）

**nodes.ts**：
- 第二轮节点数量权重：1节点 8 / 2节点 44 / 3节点 48 → 1节点 5 / 2节点 55 / 3节点 40
- 第二轮精英战基础权重：5.1 → 5.8（提升 14%）
- 第二轮精英战 `battleCatchupBonus`：1.8 → 2.0
- 第二轮精英战 `routeBonuses`：仅 crit 有加成 → crit/pierce/dash 三路线均有 1.1 加成
- 第三轮节点数量权重：1节点 12 / 2节点 46 / 3节点 42 → 1节点 8 / 2节点 52 / 3节点 40

### 验收判断

**构建验证**：✅ `npm run build` 通过

**潜在风险**：
1. 经验值提升 15% 可能导致升级过快
2. 磁吸范围提升 12% 可能让 XP 过于自动
3. 第二轮精英权重提升 14% 可能让中段过早高压

**当前判断**：
- 参数改动属于小步调整，方向合理
- 需要人工实机 2-3 局验证升级节奏、拾取自动化程度和中段精英压力
- 如果验证失败，只回调失败项，不扩大修改

## 表现层小步补强

### 1. 画面读局优化

**命中反馈增强**：
- 将敌人命中闪烁时间从 0.22 秒缩短到 0.18 秒
- 目标：让命中反馈更快更明显，提升打击满足感

**当前状态**：
- 玩家弹道、敌方弹道、XP 球、危险圈已有明确层级
- 命中闪烁、击杀 pulse、拾取连线已接通
- `crit / pierce / dash` 三路线已有基础视觉签名

### 2. 音效层次验证

**当前 duck 优先级**（从高到低）：
- `hurt`：0.46（最高优先级）
- `pressure`：0.29-0.35（Boss 更高）
- `kill / crit`：0.22
- `nearMiss`：0.2
- `enemyShot`：0.16-0.22（高压时更高）
- `hit`：0.14
- `shoot`：0.05

**判断**：
- 危险链（hurt / nearMiss / pressure / enemyShot）优先级高于主反馈链（shoot / hit / kill / pickup）
- 三路线音色差异已接通（crit 偏升温聚焦，pierce 偏冷线贯穿，dash 偏擦身折返）
- 音效层次合理，无需调整

### 3. 操控手感验证

**当前机制**：
- 移动起步：`playerMoveBoostSec = 0.2`
- 反向转向：`playerTurnBurstSec = 0.16 + pierceFlowRatio * 0.03`
- 拾取后承接：`playerMoveBoostSec` 和 `playerTurnBurstSec` 在拾取后自动延长
- 击杀后追击：通过 `killFlowCount` 和 flow chain 机制承接

**判断**：
- 操控手感已有完整承接机制
- 移动起停、反向转向、拾取吸附、击杀后追击均已接通
- 无需调整

### 4. 通俗提示验证

**当前文案**：
- 开始页：「移动躲弹，自动开火。打倒敌人，拾取能量，撑到 Boss。」
- 暂停页：显示当前阶段、路线、目标、进度
- 结算页：显示路线、生存时间、等级、节点数
- 升级卡：已接通 tooltip 声明式提示

**判断**：
- UI 文案已经通俗易懂
- 开始页、暂停页、结算页均符合"玩家能读懂"的标准
- 无需调整

## 实际修改文件

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| `src/scenes/GameScene.ts` | 视觉反馈优化 | 命中闪烁时间从 0.22 秒缩短到 0.18 秒 |

## 验证结果

### 构建验证
```bash
npm run build
```
✅ **通过** - 无类型错误，构建成功

### 修改统计
```bash
git diff --stat
```
- src/scenes/GameScene.ts: 1 行修改

## 本轮结论

### 74fa9bd 参数验收

**可接受**：
- 参数改动方向合理，属于小步调整
- 构建通过，无代码错误
- 需要人工实机验证升级节奏、拾取自动化和中段精英压力

**残留风险**：
- 经验值提升可能过快
- 磁吸范围提升可能过于自动
- 第二轮精英权重提升可能中段过压

### 表现层补强

**已完成**：
- 命中反馈更快更明显（0.22s → 0.18s）
- 音效层次验证通过（危险链优先级高于主反馈链）
- 操控手感验证通过（起步、转向、拾取、击杀承接均已接通）
- 通俗提示验证通过（开始页、暂停页、结算页文案清楚）

**未做**：
- 未继续加数值
- 未新增 Boss / 精英 / 路线
- 未重写 RunEngine
- 未重开主流程
- 未做整套 UI 主题重做

## 下一步建议

1. **人工实机验证**：游玩 2-3 局，重点观察：
   - 前 2 分钟升级频率是否过快
   - XP 拾取是否过于自动
   - 第二轮精英是否过早或过强
   - 普通战"击杀 -> 掉落 -> 拾取 -> 继续追击"是否更顺

2. **如需回调**：
   - 只回调失败项，不扩大修改
   - 如升级过快，只回调 `balance.ts` 经验值系数
   - 如拾取过自动，只回调 `balance.ts` 磁吸范围
   - 如精英过压，只回调 `nodes.ts` 精英权重

3. **如验证通过**：
   - 继续作品集材料整理
   - 补充截图、短视频脚本和 A/B 展示
   - 暂不新增 Boss / 精英 / 路线

## 最大残留风险

1. **参数副作用未完全验证**：需要人工实机 2-3 局确认升级节奏、拾取自动化和中段精英压力
2. **表现层优化幅度较小**：本轮只做了命中闪烁时间调整，其他表现层已经比较完善
3. **作品集材料仍需整理**：当前暂缓，等游戏体验更稳后再继续
