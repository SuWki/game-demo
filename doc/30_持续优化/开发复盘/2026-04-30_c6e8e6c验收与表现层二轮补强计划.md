# 2026-04-30 c6e8e6c 验收与表现层二轮补强计划

## 本轮输入

Kimi 已完成 `74fa9bd 参数验收与表现层反馈小步补强`，提交为 `c6e8e6c`。

本轮结果：

- `npm run build` 通过。
- full-flow QA 通过，`consoleErrors=[]`。
- 自然样本截图确认画面基础可读。
- `GameScene.ts` 将敌人命中闪烁窗口从 `0.22s` 缩短到 `0.18s`。
- `PilotAudio.ts`、`OverlayController.ts`、`balance.ts`、`nodes.ts` 主要为读取和验收，没有实际代码改动。

## Codex 验收判断

这轮能证明 `74fa9bd` 没有立即破坏构建和 full-flow，方向是正的。但表现层实际落地幅度偏小，目前还不能说“表现层与手感已经打磨完成”。

合理之处：

- 没有继续盲目增强玩家数值。
- 保留了 `74fa9bd` 的小步参数，没有因为担心风险直接回滚。
- 通过 full-flow 和自然样本确认没有明显崩坏。
- 命中闪烁缩短能让命中反馈更干脆，属于低风险小步优化。

不足之处：

- 表现层实际代码改动只有 1 行，尚未覆盖弹道层级、击杀爆点、XP 拾取反馈、危险提示、路线签名和音效人工听感。
- 仍缺人工实机 2~3 局补验，无法确认升级节奏、XP 自动化、中段精英压力和 Boss 安全窗体感。
- 本次提交包含 `output/qa` 产物。后续应避免默认提交 `output/`，只在任务明确要求时提交展示资产或截图索引。
- 复盘里提到“验证通过后继续作品集材料整理”，这与当前用户方向冲突。当前仍应继续游戏本体打磨，作品集继续后置。

## 下一步原则

下一轮不是继续加数值，也不是转作品集，而是做：

1. 人工实机 2~3 局补验。
2. 表现层第二轮小步补强。

如果人工实机发现参数副作用，只回调失败项：

- 升级过快：只回调经验值公式。
- XP 过于自动：只回调磁吸范围或拾取范围。
- 第二轮过压：只回调第二轮精英权重。
- Boss 安全窗不可读：只回调 Boss 安全窗/弹幕节奏，不扩新 Boss。

如果参数没有明显副作用，不再继续调 `nodes.ts / balance.ts`，直接转向表现层。

## 表现层二轮补强重点

优先级从高到低：

- `GameScene.ts`：玩家弹道、敌方弹道、XP、危险区域、命中、击杀、拾取、路线签名的层级关系。
- `PilotAudio.ts`：人工听感下的 `shoot / hit / kill / pickup` 与 `hurt / nearMiss / pressure / enemyShot` 区分度。
- `OverlayController.ts`：开始页、升级、暂停、结算、局内提示是否真正通俗。
- `style.css`：只做必要层级、可读性和面板遮挡修正。

本轮不追求大改，只要求出现可见变化：

- 至少让一个主反馈链更清楚，例如命中到击杀的视觉/音频连续性。
- 至少让一个危险反馈更清楚，例如 nearMiss、pressure 或 Boss 安全窗提示。
- 至少让一个通俗提示更贴近玩家语言。

## 本轮明确不做

- 不做作品集材料。
- 不新增 Boss、精英或路线。
- 不重写 `RunEngine`。
- 不重开主流程。
- 不继续盲目增强玩家数值。
- 不提交无关 `output/` 文件。
- 不用 UI 文案掩盖战斗反馈问题。
