# 2026-05-03 Boss 收尾 UI 修复与 QA 探针修正

## 本轮目标

接手 Kimi 未完成的 Boss 收尾 UI 问题，修复 `bossEnding` 状态进入后玩家看不到收尾画面的缺口，并让 Boss 收尾 QA 脚本能够真实验证 DOM。

## 修改内容

### `src/scenes/GameScene.ts`

- 在真实生效的 `syncOverlay()` 中补入 `bossEnding` 分支。
- 当 `state.status === 'bossEnding'` 且存在 `state.bossEnding` 时，调用 `OverlayController.showBossEnding()`。
- 保留 Boss 收尾期间的画面渲染逻辑，不重做结算页。

### `tools/qa-boss-ending.mjs`

- 不再依赖不存在的 `[data-debug-state]`。
- 不再依赖不存在的 `window.gameScene`。
- 改为使用现有 QA 入口 `window.__pilotQaForceBoss()` 强制进入 Boss 战。
- 改为使用 `window.__pilotBattleDebug()` 判断是否进入 `bossEnding`。
- 捕获 `.boss-ending-screen` 文本和截图，作为是否真实显示的验收依据。

## 验证结果

- `npm run build`：通过。
- `node tools/audit-route-upgrade-value.mjs`：硬违规 0，超预算 0，硬规则健康度 100%，预算健康度 100%。
- `node tools/qa-current-version.mjs`：通过，`consoleErrors=[]`，结果页、暂停页、音量面板均正常。
- `$env:PILOT_QA_BOSS='boss-lockdown'; node tools\qa-boss-ending.mjs`：通过。
  - `bossEndingSeen=true`
  - 文本包含“机体失效 / Boss 未击破”
  - `consoleErrors=0`
- 三个 Boss 定向 QA 均能采到安全区数据：
  - `boss-bastion`: 安全区 5，区外伤害 14，区内清弹 34，面板遮挡 0。
  - `boss-hunt`: 安全区 4，区外伤害 14，区内清弹 2，面板遮挡 0。
  - `boss-lockdown`: 安全区 4，区外伤害 14，区内清弹 14，面板遮挡 1。

## 当前结论

Boss 收尾失败路径已经可以签收。Boss 胜利路径使用同一个 `showBossEnding()` 入口，但仍建议后续补一次击败 Boss 的实机或专项样本，确认“首领击破 / 任务完成”节奏。

Boss 定向 QA 目前说明安全区数据可以真实采样，但 `boss-lockdown` 仍出现 `panelBlockedMoments=1`，不能把三 Boss QA 写成完全无面板遮挡通过。下一轮需要复查这是 QA 入口残留面板、脚本判断过严，还是实际 Boss 开场仍可能被节点/升级面板打断。

## 下一步建议

1. 做 `crit / pierce / dash` 各 1 局人工实机。
2. 复查 `boss-lockdown` 的 `panelBlockedMoments=1`，确认是否需要修 QA 脚本或运行时开场面板。
3. 重点记录每局拿到第几张路线牌开始明显成型。
4. 重点观察精英/Boss 是否仍然被 1 到 2 张牌快速击穿。
5. 重点观察敌人状态标记是否帮助读局，还是变成新的视觉噪音。

## QA 产物规则

本轮生成的 `output/qa` 截图和 summary 只作为本地验证证据，不提交到 Gitee。
