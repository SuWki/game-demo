# 2026-05-02 验收工具修复与路线重构复验

## 本轮复验目标

复验 Kimi 提交的 `967f9b7 fix: 工具修复与路线牌第一轮重构`，确认以下内容是否可以作为下一轮开发基线：

- Boss QA 脚本是否从语法错误恢复为真实可用。
- Boss 安全区、区外伤害、区内清弹数据是否能被采样。
- 路线牌违规属性是否清零。
- 当前文档是否足够让新对话不依赖历史上下文继续接手。

## 复验命令与结果

- `npm run build`：通过。
- `node tools/audit-route-upgrade-value.mjs`：通过，中文输出正常。
- `node tools/qa-current-version.mjs`：通过，`consoleErrors=[]`，`resultSeen=true`，`pauseSeen=true`，`volumePanelSeen=true`。
- `PILOT_QA_BOSS=boss-bastion node tools/qa-boss-directed-v2.mjs`：脚本可运行，但 Boss 安全区数据采样失败。

## 路线牌审计结论

- 总路线牌：64 张。
- 违规路线牌：0 张。
- 审计健康度：100%。
- 仍需关注：审计报告中仍有 3 张超预算路线牌，说明当前只是完成“禁用属性清理”，还没有完成完整价值均衡。

当前结论：

- 可以认为“路线牌混入 damage / multishot / regeneration 等明显违规属性”的第一轮清理已完成。
- 不能认为“流派构筑重构已完成”。当前大部分路线牌仍主要是数值型机制属性，例如 `critChance`、`critMultiplier`、`pierce`、`projectileSpeed`、`dashPulseDamage`、`dashInvulnerability`。
- 下一轮应进入“构筑第二轮”：把路线牌从数值增强推进到触发条件、连锁收益、敌人状态、Boss/精英解法。

## Boss QA 复验结论

本次 Boss directed QA 输出：

- `consoleErrors=0`
- `bossSafeWindowMoments=0`
- `outsideSafeDamageTickCount=0`
- `insideSafeProjectileClears=0`
- `bossAliveDurationSec=98.6s`

截图显示 Boss 战中途停在升级选择面板，说明脚本没有真正持续采样 Boss 安全区。

判断：

- `qa-boss-directed-v2.mjs` 的语法错误已修复。
- `outsideSafeDamageTimerSec` / `outsideSafeDamageTickCount` 字段拆分方向正确。
- 但 Boss QA 仍不能签收，因为脚本没有正确关闭通用选择面板。

需要修复：

- `closeUpgradeOrOverlayIfPresent()` 必须支持 `[data-choice]`，而不只检查 `[data-upgrade-choice]` / `[data-event-option]`。
- Boss QA 必须在采样循环中检测面板是否遮挡游戏，并关闭后继续采样。
- Boss QA 成功标准必须是 `bossSafeWindowMoments > 0`，并至少采到 `outsideSafeDamageTickCount > 0` 或能解释为什么玩家一直在安全区内。

## 流程问题

`967f9b7` 提交包含大量 `output/qa` 截图和 summary。后续协作规则需要更硬：

- QA 产物默认不提交。
- 如果需要保留 QA 证据，优先写入复盘摘要，不提交整批截图。
- 只有用户明确要求“提交 QA 截图/样本”时，才允许提交 `output/qa`。

## 下一轮开发目标

下一轮优先级应为：

1. 修复 Boss QA 面板关闭逻辑，确保安全区/区外伤害/区内清弹真实采样。
2. 根据真实 Boss QA 数据修 Boss 安全区触发频率与区外惩罚，不允许只凭感觉调数值。
3. 做路线构筑第二轮，把 `crit / pierce / dash` 从“数值机制属性”推进为“触发条件 + 敌人状态 + 连锁 payoff”。
4. 新增 Boss 战收尾画面，区分“Boss 击败成功”和“机体失效失败”，避免突然切通用结算。
5. 清理流程纪律：代码提交不带 `output/qa` 大量产物，最终汇报必须中文。

## 下一轮不建议做

- 不新增 Boss。
- 不新增路线。
- 不做作品集材料。
- 不继续批量堆素材。
- 不用大幅加血或加弹幕替代 Boss 机制修复。
- 不把路线构筑第二轮简化成继续改 `upgrades.ts` 数值。
